enabled: true
js_inline: "(function() {\n    var theme = localStorage.getItem('helios-theme') || 'system';\n    var isDark = theme === 'dark' || (theme === 'system' && window.matchMedia('(prefers-color-scheme: dark)').matches);\n    var cardTheme = isDark ? 'dark' : 'light';\n\n    var embeds = document.querySelectorAll('.embedly-card');\n    for (var i = 0; i < embeds.length; i++) {\n        embeds[i].setAttribute('data-card-theme', cardTheme);\n    }\n\n    // Watch for dark class changes on <html> via MutationObserver,\n    // since the Helios theme toggle buttons directly manipulate the\n    // class list without dispatching the helios-theme-change event.\n    // Only start observing after page has fully loaded, so we skip\n    // the initial class mutations during page setup and only react\n    // to user-initiated theme toggles.\n    if (embeds.length > 0) {\n        window.addEventListener('load', function() {\n            var observer = new MutationObserver(function() {\n                observer.disconnect();\n                window.location.reload();\n            });\n            observer.observe(document.documentElement, { attributes: true, attributeFilter: ['class'] });\n        });\n    }\n})();\n"
css_inline: ".prose blockquote.announcement {font-style: normal}\n.prose blockquote.announcement :is(h1,h2,h3) {margin: 0; line-height: 1.3}\n.prose blockquote.announcement h1 {margin-bottom: 0.25rem}\n.prose blockquote.announcement h2 {border-bottom: none; padding-bottom: 0; margin-bottom: 0.5rem}\n.prose blockquote.announcement h3 {margin: 0.75rem 0 0.25rem}\n.prose blockquote.announcement h2+h3 {margin-top: 0.25rem}\n.prose blockquote.announcement h1 {font-size: calc(2.25em * 0.8)}\n.prose blockquote.announcement h2 {font-size: calc(1.5em * 0.8)}\n.prose blockquote.announcement h3 {font-size: calc(1.25em * 0.8)}\n.prose blockquote.announcement h4 {font-size: calc(1em * 0.8)}\n.prose blockquote.announcement h5 {font-size: calc(0.875em * 0.8)}\n.prose blockquote.announcement h6 {font-size: calc(0.75em * 0.8)}\n.prose :is(h1,h2,h3,h4,h5,h6) {margin: 0; line-height: 1.3}\n.prose h1 {margin-bottom: 0.5rem}\n.prose h2 {border-bottom: none; padding-bottom: 0; margin-bottom: 1rem}\n.prose :is(h3,h4,h5,h6) {margin: 1.5rem 0 0.5rem}\n.prose :is(h2,h3,h4,h5)+:is(h3,h4,h5,h6) {margin-top: 1rem}\n/* Font Awesome icon spacing */\n.prose i[class^=\"fa\"] {margin-right: 0.35em;}\n/* Responsive iframe/video containers */\n.html5-video-container {position: relative; width: 100%; aspect-ratio: 16 / 9; overflow: hidden}\n.html5-video-container iframe {position: absolute; top: 0; left: 0; width: 100%; height: 100%; border: none}"
js_assets:
  -
    url: 'https://cdn.embedly.com/widgets/platform.js'
